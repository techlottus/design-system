import { Meta, Preview } from '@storybook/blocks';
import CheckBox from './CheckBox'
import CheckBoxLabel from './CheckBoxLabel'
import CheckBoxLink from './CheckBoxLink'

<Meta title="MDX|CheckBox" component={CheckBox} />

# CheckBox

Checkboxes empower users to effortlessly select one or multiple items from a given set, enhancing their control and flexibility within the interface.

## Installation

To implement this component we use Headless UI which allows us to seamlessly combine unstyled but fully accessible UI components in our projects, meticulously designed to harmonize with the Tailwind CSS integration.

To kick off, initiate the installation of Headless UI via npm.

`npm install @headlessui/react`

Here are three examples showcasing the use cases for the radio group and its functionality:

## CheckBox

 <CheckBox /> 

## CheckBoxLabel

 <CheckBoxLabel /> 

## CheckBoxLink

 <CheckBoxLink /> 

For further details, please visit <a className="cursor-pointer" href="https://headlessui.com/" target="_blank">https://headlessui.com/</a>
