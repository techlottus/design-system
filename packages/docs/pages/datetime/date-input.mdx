import { DateInput } from "@exponentialeducation/datetime/src";

# Date Picker

Documentation of the `<DateInput />` component. Learn how to use it and its available props.

## Installation

To get started, install `@exponentialeducation/betomic` via yarn:

```bash
# Yarn
yarn add @exponentialeducation/datetime
```

## Basic example

The DateInput allows users to enter a date either through an input field, or by choosing a single date from a monthly calendar overlay.
`DateInput` is built on top of the `react-day-picker` library.

```jsx highlight=9
import { DateInput } from "@exponentialeducation/datetime";

function IndexPage() {

  return (
    <main>
      {/* ... */}

      <DateInput />

      {/* ... */}
    </main>
  )
}
```

The previous example describes the simplest way of using the DateInput component, it uses default `props` and renders a component like the following:

<DateInput />


## Props

<table className="min-w-full divide-y divide-gray-200">
  <thead>
    <tr>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        prop
      </th>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        description
      </th>
    </tr>
  </thead>
  <tbody className="divide-y divide-gray-200">
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `canChangeMonth?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **boolean** *= true*<br/>
        Enable the navigation between months.<br/>
        If set to `false` outside days are hidden to avoid navigation to undesired dates.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `defaultValue?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **Date**<br/>
        Initial date the calendar will display as selected.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `disabled?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **boolean** *= false*<br/>
        Defines if the date input is non-interactive.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `disabledDates?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **Date | Date[]**<br/>
        Date(s) that should appear as disabled.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `format?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **"L" | "l" | "LL" | "ll"** *= "L"*<br/>
        Defines how the date will be formtated and parsed. It follows the *Multiple Locale Support* convention by [moment.js](https://momentjs.com/).
        It accepts formats for date selection, not time.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `initialMonth?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **Date**<br/>
        The initial month the calendar displays.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `label?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **string**<br/>
        Label of the DateInput FormGroup.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `labelFor?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **string**<br/>
        Sets the `for` attribute of the DateInput FormGroup and the `id` attribute of its corresponding DateInput Input.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `labelHelper?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **boolean** *= false*<br/>
        Shows a FormGroup JSX `rightElement` that consists of a `<HelperIcon />` rendered on the right side.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `maxDate?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **Date**<br/>
        The latest date the user can select.<br/>
        User cannot navigate after this date in the calendar, and dates after this one are disabled, meaning non-selectable.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `minDate?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **Date**<br/>
        The earliest date the user can select.<br/>
        User cannot navigate before this date in the calendar, and dates before this one are disabled, meaning non-selectable.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `onChange?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **(selectedDate: Date) => void**<br/>
        Called when the user selects a day in the calendar.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `required?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **boolean** *= false*<br/>
        A `*` decoration for the DateInput FormGroup label. Use it along with `label` prop.
      </td>
    </tr>

    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `showOutsideDays?`
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        **boolean** *= true*<br/>
        Display the days falling outside the current month. These days are selectable unless `minDate` or `maxDate` props are set.
      </td>
    </tr>

  </tbody>
</table>