import Callout from 'nextra-theme-docs/callout'
import { HelloWorld } from '@bedu-tmp/betomic/src'

# Getting Started

<Callout emoji="💡">
  If you want to use `@bedu-tmp/betomic` you must have a latest version from `react`
</Callout>

Inside your React project directory, run the following:

## Installation

```plaintext
yarn add @bedu-tmp/betomic
```
Or with npm:

```plaintext
npm install @bedu-tmp/betomic
```

## Quick Start

```jsx
import { HelloWorld } from '@bedu-tmp/betomic'

const Index = () => {
  return (
    <main>
      <HelloWorld />
    </main>
  )
}
```

## Example

```jsx
import { HelloWorld } from '@bedu-tmp/betomic'

const Index = () => {
  return (
    <main>
      <HelloWorld />
    </main>
  )
}
```

the previous example render:

<HelloWorld />
