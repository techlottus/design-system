import {
  BasicAlert,
  TypesAlert,
  ClosableAlert,
  } from "../../components/AlertExample";

# Alert

Documentation for the `<Alert />` component. Learn about the available props and how to use it.

## Installation

To get started, install `@exponentialeducation/betomic` via `yarn`:

```bash
# Yarn

yarn add @exponentialeducation/betomic
```

## Basic Example

An `Alert` notify users of important information.

```jsx highlight=3,10-12
 {/* ... */}

import { Alert } from "@exponentialeducation/betomic";

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <Alert>
        Default alert example
      </Alert>

      {/* ... */}
    </main>
  )
}
```
<BasicAlert />

## Variants

You can change the default style, passing a valid `type` prop:

```jsx highlight=3,10
 {/* ... */}

import { Alert } from "@exponentialeducation/betomic";

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <Alert type="success">
        Success are to notify...
      </Alert>

      {/* ... */}
    </main>
  )
}
```
<TypesAlert />

## Closable

```jsx highlight=3,10
 {/* ... */}

import { Alert } from "@exponentialeducation/betomic";

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <Alert type="success" closable>
        Success are to notify...
      </Alert>

      {/* ... */}
    </main>
  )
}
```
<ClosableAlert />

## Props

<table className="min-w-full divide-y divide-gray-200">
  <thead>
    <tr>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        prop
      </th>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        description
      </th>
    </tr>
  </thead>
  <tbody className="divide-y divide-gray-200">
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        bordered
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `boolean` *= true* <span className="block">Apply a border style</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        children
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `node` <span className="block">Content will be render</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        closable?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `boolean` = *"false"* <span className="block">Shows a decoration, it can close the alert</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        type?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `"default" | "error" | "success" | "warning" | "info"` *= default* <span className="block">Visual styles to apply to `<Alert />`</span>
      </td>
    </tr>
  </tbody>
</table>
