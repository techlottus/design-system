import { InputGroupValidationExample, InputGroupRightExample, InputGroupBasicExample, InputGroupLeftExample, InputGroupComplexExample, InputGroupSearchExample, InputGroupDarkExample } from "../../components/InputGroupExample";

# Input Group

Documentation for the `<InputGroup />` component. Learn about the available props and how to use it.

## Installation

To get started, install `@bedu-tmp/betomic` via `yarn`:

```bash
# Yarn

yarn add @bedu-tmp/betomic
```

## Basic Example

`InputGroup` are a basic building block used to render text inputs across many components.
They allow you to add icons and buttons within a text input to expand its appearance and functionality.
For example, you might use an input group to build a visibility toggle for a password field.

```jsx highlight=3,10
 {/* ... */}

import { InputGroup } from '@bedu-tmp/betomic';

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <InputGroup placeholder="Basic example" />

      {/* ... */}
    </main>
  )
}
```

<InputGroupBasicExample />

## Left decoration

```jsx highlight=3,10-13
 {/* ... */}

import { InputGroup } from '@bedu-tmp/betomic';

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <InputGroup
        placeholder="Left decoration"
        leftElement={<EyeIcon className="w-4" />}
      />

      {/* ... */}
    </main>
  )
}
```

<InputGroupLeftExample />

## Right decoration

```jsx highlight=3,10-13
 {/* ... */}

import { InputGroup } from '@bedu-tmp/betomic';

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <InputGroup
        placeholder="Right decoration"
        leftElement={<EyeIcon className="w-4" />}
      />

      {/* ... */}
    </main>
  )
}
```

<InputGroupRightExample />

## Both decorations

```jsx highlight=1,10-15
import { InputGroup } from '@bedu-tmp/betomic';

function IndexPage() {
  return (
    <div className="w-full mt-5">
      {/* ... */}

      <InputGroup
        className="pl-18 pr-20"
        leftElement={
          <span className=" text-surface-300 select-none">https://</span>
        }
        rightElement={
          <span className="text-surface-300 select-none">.bedu.org</span>
        }
      />

      {/* ... */}
    </div>
  );
}
```

<InputGroupComplexExample />

## With validations

```jsx highlight=3,11,16
 {/* ... */}

import { InputGroup } from '@bedu-tmp/betomic';

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <InputGroup
        isValid={false}
        placeholder="Valid false example"
        rightElement={<EyeIcon className="w-4" />}
      />
      <InputGroup
        isValid
        placeholder="Valid example"
        rightElement={<CheckIcon className="w-4" />}
      />

      {/* ... */}
    </main>
  )
}
```

<InputGroupValidationExample />


## Other

```jsx highlight=1,8-14
import { InputGroup } from '@bedu-tmp/betomic';

function IndexPage() {
  return (
    <div className="w-full mt-5">
      {/* ... */}

      <InputGroup
        type="search"
        placeholder="Search example..."
        leftElement={
          <SearchIcon className="w-4" />
        }
      />

      {/* ... */}
    </div>
  );
}
```

<InputGroupSearchExample />

## Dark mode

By enabling dark mode in an app that implements an `<InputGroup />` or by using the TailwindCSS `dark` class directly on its container as well in the right/left elements, the component styles accordingly.<br/>

```jsx highlight=1,5,8-15
import { InputGroup } from '@bedu-tmp/betomic';

function IndexPage() {
  return (
    <div className="w-full mt-5 dark">
      {/* ... */}

      <InputGroup
        type="search"
        className="h-12"
        placeholder="Search example..."
        leftElement={
          <SearchIcon className="w-4" />
        }
      />

      {/* ... */}
    </div>
  );
}
```

<InputGroupDarkExample />

## Props

Extends `HTMLInputElement`props

<table className="min-w-full divide-y divide-gray-200">
  <thead>
    <tr>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        prop
      </th>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        description
      </th>
    </tr>
  </thead>
  <tbody className="divide-y divide-gray-200">
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        isValid?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `boolean | null` <span className="block">Add styles to validate an `input`, default value it's `null`</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        type?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `string` <span className="block">A valid input type, by default it's `text` </span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        leftElement?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `JSX` <span className="block">The element will be render in the left side</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        rightElement?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `JSX` <span className="block">The element will be render in the right side</span>
      </td>
    </tr>
  </tbody>
</table>
