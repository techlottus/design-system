import { InputGroupBasicExample, InputGroupLeftExample, InputGroupComplexExample, InputGroupSearchExample } from "../../components/InputGroupExample";

# Input Group

Documentation for the `<InputGroup />` component. Learn about the available props and how to use it.

## Installation

To get started, install `@exponentialeducation/betomic` via `yarn`:

```bash
# Yarn

yarn add @exponentialeducation/betomic
```

## Basic Example

`InputGroup` are built using the `InputGroup.Decoration` component.

The `InputGroup.Decoration` place the decoration element in `right` or `left` position,
by default the elements are placed in the left side.

`InputGroup` are a basic building block used to render text inputs across many components.
They allow you to add icons and buttons within a text input to expand its appearance and functionality.
For example, you might use an input group to build a visibility toggle for a password field.

```jsx highlight=3,10-15
 {/* ... */}

import { Input, InputGroup } from '@exponentialeducation/betomic';

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <InputGroup>
        <InputGroup.Decoration position="right">
          <EyeIcon className="w-4 h-full" />
        </InputGroup.Decoration>
        <Input type="password" className="pr-10" placeholder="Right decoration" />
      </InputGroup>

      {/* ... */}
    </main>
  )
}
```

<InputGroupBasicExample />

## Left decoration

```jsx highlight=3,10-15
 {/* ... */}

import { Input, InputGroup } from '@exponentialeducation/betomic';

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <InputGroup>
        <InputGroup.Decoration>
          <CheckIcon className="w-4 h-full" />
        </InputGroup.Decoration>
        <Input type="text" className="pl-10" placeholder="Left decoration" />
      </InputGroup>

      {/* ... */}
    </main>
  )
}
```

<InputGroupLeftExample />

## Both decorations

```jsx highlight=1,9,15
import { Input, InputGroup } from '@exponentialeducation/betomic';

function IndexPage() {
  return (
    <div className="w-full mt-5">
      {/* ... */}

      <InputGroup>
        <InputGroup.Decoration position="left">
          <span className="text-surface-300">
            http://
          </span>
        </InputGroup.Decoration>
        <Input placeholder="Example" className="px-18" />
        <InputGroup.Decoration position="right">
          <span className="text-surface-300">
            .bedu.org
          </span>
        </InputGroup.Decoration>
      </InputGroup>

      {/* ... */}
    </div>
  );
}
```

<InputGroupComplexExample />

## Other

```jsx highlight=1,8-13
import { Input, InputGroup } from '@exponentialeducation/betomic';

function IndexPage() {
  return (
    <div className="w-full mt-5">
      {/* ... */}

      <InputGroup>
        <InputGroup.Decoration>
          <SearchIcon className="w-4 text-surface-700"/>
        </InputGroup.Decoration>
        <Input type="search" className="pl-10" placeholder="Search example..." />
      </InputGroup>

      {/* ... */}
    </div>
  );
}
```

<InputGroupSearchExample />

## Props

### InputGroup

The main component

<table className="min-w-full divide-y divide-gray-200">
  <thead>
    <tr>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        prop
      </th>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        description
      </th>
    </tr>
  </thead>
  <tbody className="divide-y divide-gray-200">
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        children
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `node` <span className="block">Content will be render</span>
      </td>
    </tr>
  </tbody>
</table>

### InputGroup.Decoration

<table className="min-w-full divide-y divide-gray-200">
  <thead>
    <tr>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        prop
      </th>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        description
      </th>
    </tr>
  </thead>
  <tbody className="divide-y divide-gray-200">
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        children
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `node` <span className="block">Content will be render</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        position?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `"left" | "right"` <span className="block">The place where the decoration element will be render, by default it's `left`</span>
      </td>
    </tr>
  </tbody>
</table>