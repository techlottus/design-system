import { DisabledInputExample, InputExample, ValidInputExample } from "../../components/InputExample";

# Input

Documentation for the `<Input />` component. Learn about the available props and how to use it.

## Installation

To get started, install `@exponentialeducation/betomic` via `yarn`:

```bash
# Yarn

yarn add @exponentialeducation/betomic
```

## When to use

A user input is needed.

```jsx highlight=1,20
import { Input } from '@exponentialeducation/betomic';
import cn from "classnames";

const inputDefaultClasses = cn(
  "rounded p-3 h-10 w-full",
  "font-rubik font-normal text-base text-surface-700 bg-surface-100 placeholder-surface-400",
  "border-2 border-transparent focus:outline-none focus:bg-white",
  "hover:border-surface-200",
);

function IndexPage() {
  const defaultClasses = cn(
    inputDefaultClasses,
    "focus:ring-primary-500 focus:ring-opacity-40 focus:ring-2 focus:border-primary-500"
  )
  return (
    <main>
      {/* ... */}

      <Input className={defaultClasses} placeholder="Placeholder" />

      {/* ... */}
    </main>
  )
}
```

## Basic example

<InputExample />

## Valid styles

```jsx highlight=15-18,24
import { Input } from '@exponentialeducation/betomic';
import cn from "classnames";

const inputDefaultClasses = cn(
  "rounded p-3 h-10 w-full",
  "font-rubik font-normal text-base text-surface-700 bg-surface-100 placeholder-surface-400",
  "border-2 border-transparent focus:outline-none focus:bg-white",
  "hover:border-surface-200",
);

function IndexPage() {
  const [isValid, setIsValid] = useState(true);
  const validInput = cn(
    inputDefaultClasses,
    {
      ["border-success-400 bg-white hover:border-success-400"]: isValid,
      ["border-error-400 bg-white hover:border-error-400"]: !isValid,
    }
  )
  return (
    <main>
      {/* ... */}

      <Input className={validInput} />

      {/* ... */}
    </main>
  )
}
```

<ValidInputExample />

## Disabled

```jsx highlight=14,18
import { Input } from '@exponentialeducation/betomic';
import cn from "classnames";

const inputDefaultClasses = cn(
  "rounded p-3 h-10 w-full",
  "font-rubik font-normal text-base text-surface-700 bg-surface-100 placeholder-surface-400",
  "border-2 border-transparent focus:outline-none focus:bg-white",
  "hover:border-surface-200",
);

function IndexPage() {
  const defaultClasses = cn(
    inputDefaultClasses,
    "bg-surface-100 pointer-events-none"
  )
  return (
    <div className="w-full mt-5">
      <Input disabled className={defaultClasses} />
    </div>
  );
}
```

<DisabledInputExample />

## Props

Extends `HTMLInputElement`props

<table className="min-w-full divide-y divide-gray-200">
  <thead>
    <tr>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        prop
      </th>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        description
      </th>
    </tr>
  </thead>
  <tbody className="divide-y divide-gray-200">
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        type?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `string` Defaults `text`
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        className?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `string` Extend the styles applied to the component
      </td>
    </tr>
  </tbody>
</table>