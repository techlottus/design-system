import {
  FormGroupExample,
  FormGroupRequiredExample,
  FormGroupRightExample,
  FormGroupHelperExample,
  FormGroupMessageExample,
  FormGroupDisabledExample } from "../../components/FormGroupExample";

# Form Group

Documentation for the `<FormGroup />` component. Learn about the available props and how to use it.

## Installation

To get started, install `@exponentialeducation/betomic` via `yarn`:

```bash
# Yarn

yarn add @exponentialeducation/betomic
```

## Basic Example

`FormGroup` are built using the `FormGroup.Message` component.

```jsx highlight=3,10-14
 {/* ... */}

import { FormGroup, InputGroup } from '@exponentialeducation/betomic';

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <FormGroup
        label="Label"
        labelFor="test-input">
        <InputGroup id="test-input" placeholder="Basic example" />
      </FormGroup>

      {/* ... */}
    </main>
  )
}
```

<FormGroupExample />

## Required decoration

```jsx highlight=3,12
 {/* ... */}

import { InputGroup, FormGroup } from '@exponentialeducation/betomic';

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <FormGroup
        label="Label"
        required
        labelFor="test-input-required">
        <InputGroup id="test-input-required" placeholder="Basic example" />
      </FormGroup>

      {/* ... */}
    </main>
  )
}
```
<FormGroupRequiredExample />

## Right element decoration

```jsx highlight=3,14-16
 {/* ... */}

import { InputGroup, FormGroup } from '@exponentialeducation/betomic';

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <FormGroup
        label="Label"
        required
        labelFor="test-input"
        rightElement={
          <HelperIcon className="w-4" />
        }>
        <InputGroup id="test-input" placeholder="Basic example" />
      </FormGroup>

      {/* ... */}
    </main>
  )
}
```

<FormGroupRightExample />

## Helper text

```jsx highlight=1,11
import { InputGroup, FormGroup } from '@exponentialeducation/betomic';

function IndexPage() {
  return (
    <div className="w-full mt-5">
      {/* ... */}

      <FormGroup
        label="Label"
        required
        helperText="This is a helper text example"
        labelFor="test-input-helper-text"
        rightElement={
          <HelperIcon className="w-4" />
        }
        >
        <InputGroup disabled id="test-input-helper-text" placeholder="Basic example" />
      </FormGroup>

      {/* ... */}
    </div>
  );
}
```

<FormGroupHelperExample />

## Form group message

```jsx highlight=3,19
 {/* ... */}

import { InputGroup, FormGroup } from '@exponentialeducation/betomic';

function IndexPage() {
  return (
    <main>
      {/* ... */}

      <FormGroup
        label="Label"
        required
        labelFor="test-input-group-message"
        rightElement={
          <HelperIcon className="w-4" />
        }
        >
        <InputGroup isValid={false} id="test-input-group-message" placeholder="Basic example" />
        <FormGroup.Message type="error">Este campo es requerido</FormGroup.Message>
      </FormGroup>

      {/* ... */}
    </main>
  )
}
```

<FormGroupMessageExample />


## Disabled

```jsx highlight=1,9
import { InputGroup, FormGroup } from '@exponentialeducation/betomic';

function IndexPage() {
  return (
    <div className="w-full mt-5">
      {/* ... */}

      <FormGroup
        disabled
        label="Label"
        required
        labelFor="test-input-group-disabled"
        rightElement={
          <HelperIcon className="w-4" />
        }
        >
        <InputGroup id="test-input-group-disabled" placeholder="Basic example" />
      </FormGroup>

      {/* ... */}
    </div>
  );
}
```

<FormGroupDisabledExample />

## Props

### FormGroup

Main component

<table className="min-w-full divide-y divide-gray-200">
  <thead>
    <tr>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        prop
      </th>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        description
      </th>
    </tr>
  </thead>
  <tbody className="divide-y divide-gray-200">
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        children?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `node` <span className="block">Content will be render</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        disabled?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `boolean` <span className="block">Styles when actions are not available</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        helperText?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `string` <span className="block">Optional helper text</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        label?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `string` <span className="block">Label of this form group.</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        labelFor?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `string` <span className="block">`id` attribute of the labelable form element that this FormGroup controls, used as `<label for>` attribute.</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        required?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `boolean` <span className="block">A `*` decoration</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        rightElement?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `JSX` <span className="block">The element will be render in the right side</span>
      </td>
    </tr>
  </tbody>
</table>

### FormGroup.Message

<table className="min-w-full divide-y divide-gray-200">
  <thead>
    <tr>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        prop
      </th>
      <th scope="col" className="border-l-0 border-r-0 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
        description
      </th>
    </tr>
  </thead>
  <tbody className="divide-y divide-gray-200">
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        message?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `string` <span className="block">Message will be render</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        show?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `boolean` <span className="block">When to display a form group message</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        type?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `"error" | "warning" | "success" | "default" | "info"` <span className="block">Message box styles, by default it's `default`</span>
      </td>
    </tr>
    <tr>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        disabled?
      </td>
      <td className="border-l-0 border-r-0 px-6 py-4 text-sm font-medium text-gray-500">
        `boolean` <span className="block">Styles when actions are not available</span>
      </td>
    </tr>
  </tbody>
</table>