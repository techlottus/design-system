{
  "name": "@exponentialeducation/betomic",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "clean": "lerna run --parallel clean",
    "compile:betomic": "lerna run compile --scope @exponentialeducation/betomic",
    "build:icons": "lerna run build --scope @exponentialeducation/iconography",
    "docs:dev": "yarn build:icons & lerna run dev --scope @exponentialeducation/docs",
    "test": "lerna run --parallel test"
  },
  "dependencies": {
    "@types/react": "^17.0.20",
    "@types/react-dom": "^17.0.9",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "typescript": "^4.4.3"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "description": "A React-based UI toolkit for web",
  "author": "Bedu Engineering <it@bedu.org>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bedu-hq/betomic.git"
  },
  "bugs": {
    "url": "https://github.com/bedu-hq/betomic/issues"
  }
}
